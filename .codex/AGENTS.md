# Codex Agent Notes

Read this first for every task.

- Keep context small and edits focused.
- Re-read when the task changes.
- Prefer one feature, one test bundle, one push.
- Never skip tests.
- Reuse shared tokens and helpers instead of duplicating styles or logic.
- Stop and report if a change looks risky or security-sensitive.
